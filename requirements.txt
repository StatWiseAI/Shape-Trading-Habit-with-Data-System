# Trading Analytics Platform — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core data stack ──────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
openpyxl>=3.1.0          # XLSX read/write

# ── Streamlit app (Step 3) ───────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0            # interactive charts

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0         # coverage reports

# add these two lines to requirements.txt
scipy>=1.10.0          # already there — no change
statsmodels>=0.14.0    # ADD THIS
